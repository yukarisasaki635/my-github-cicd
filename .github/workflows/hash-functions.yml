name: Hash Functions
on: push
jobs:
  hash:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - run: echo "${HASH}"
      env:
        HASH: ${{ hashFiles('.github/workflows/*.yml') }}